<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8" />
  <title>{{title}}</title>
  <style>
    :root {
      --legacisPurple: #6104C0;
      --legacisLightGreen: #E2FFE9;
      --legacisPink: #FA2EF3;
      --legacisGreen: #4AEDB9;
      --legacisBlue: #8036F2;
      --legacisLight: #F1FFFA;
    }
    body { font-family: 'Lato', Helvetica, Arial, sans-serif !important; }
    td, th {
      font-size: 16px !important;
      color: #374151 !important;
      line-height: 1.5 !important;
    }
    p { font-size: 16px !important; }
    a.text-link {
      color: #8036F2 !important;
      text-decoration: underline !important;
      font-weight: 500;
      transition: color 0.2s;
    }
    a.text-link:hover {
      color: #6104C0 !important;
    }
    .btn {
      display: inline-block;
      background: linear-gradient(90deg, var(--legacisPurple) 0%, var(--legacisGreen) 100%);
      padding: 14px 36px;
      border-radius: 8px;
      text-decoration: none;
      font-weight: bold;
      font-size: 1rem;
      margin-top: 24px;
      box-shadow: 0 2px 8px var(--legacisLightGreen);
      letter-spacing: 1px;
      transition: background 0.2s, box-shadow 0.2s;
      border: none;
    }
    .btn:hover {
      background: linear-gradient(90deg, var(--legacisGreen) 0%, var(--legacisPurple) 100%);
      box-shadow: 0 4px 16px var(--legacisLightGreen);
    }
    .details-table {
      width: 100%;
      border-collapse: collapse;
      margin: 24px 0;
    }
    .details-table th, .details-table td {
      text-align: left;
      padding: 8px 12px;
      border-bottom: 1px solid #f3f4f6;
    }
    .details-table th {
      background: #f9fafb;
      color: #374151;
      font-weight: 600;
    }
  </style>
</head>
<body style="margin:0; border-radius:10px; background-color: #fef5ff; padding: 24px; font-family: Lato, 'Helvetica Neue', Arial, 'sans-serif';">
  <div style="max-width: 600px; margin: 0 auto; border-radius:10px; background-color: #fff; box-shadow: 0 2px 12px rgba(0, 0, 0, 0.1);">
    {{> header}}
    <div style="padding: 24px;">
      <h3 style="margin-top:0; color: #6104C0;">Hello {{customerName}},</h3>
      <p>
        Thank you for your purchase! We have received your payment for <strong>{{serviceName}}</strong>.
      </p>
      <table class="details-table">
        <tr>
          <th>Service Name</th>
          <td>{{serviceName}}</td>
        </tr>
        <tr>
          <th>Plan</th>
          <td>{{planDuration}}</td>
        </tr>
        <tr>
          <th>Plan Type</th>
          <td>{{planType}}</td>
        </tr>
        <tr>
          <th>Order ID</th>
          <td>{{orderId}}</td>
        </tr>
        <tr>
          <th>Transaction ID</th>
          <td>{{transactionId}}</td>
        </tr>
        <tr>
          <th>Amount Paid</th>
          <td>{{amount}} {{currency}}</td>
        </tr>
        <tr>
          <th>Payment Method</th>
          <td>{{paymentMethod}}</td>
        </tr>
        <tr>
          <th>Purchase Date</th>
          <td>{{purchaseDate}}</td>
        </tr>
      </table>
      <p style="margin: 16px 0; width: 100%;">
       To view more detail about your transaction, visit your profile: <a href="{{profileUrl}}" class="text-link">View Profile</a>
      </p>
      <div style="display: flex; justify-content: center; margin: 32px 0; width: 100%;">
        <a href="{{dashboardUrl}}" class="btn">Go to Dashboard</a>
      </div>
      <p style="margin-top:32px;">
        If you have any questions, please <a href="mailto:support@legaciscapital.com" class="text-link">contact our support team</a>.
      </p>
    </div>
    {{> footer}}
  </div>
</body>
</html>